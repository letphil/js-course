/**
 * to log things in the dev-tools or shell
 * console.log is commonly used
 * console in javascript is a class or object that has methods
 * most commonly used is .log -> example console.log('string given')
 * console.log can also take as many arguments or parameters needed
 * to do so:
 *
 * console.log(1, 'string', true, { key: 'value' }, [1, 2, 3, 4, 5])
 *
 * other commonly used are
 * .warn
 * .error
 *
 * if wanting more
 * ref:
 * https://developer.mozilla.org/en-US/docs/Web/API/console
 */

// 1. console.log a string
// console.log()

// 2. console.log a number
// console.log()

// 3. console.log a boolean
// console.log

/**
 * to capture data-types into a variable we must initiate a key word ->
 * var, const, let
 * followed by any text without spaces followed by equal sign then can assign any data type
 *
 * example -> var letPhil = 'coding is fun'
 *
 * usage -> console.log('letPhil =', letPhil)
 *
 * extra -> var is the older way to assign variables
 * since es6 https://www.w3schools.com/js/js_es6.asp
 *
 * more common to use
 * const and let
 * const -> when variables do not get reassign
 *  example ->
 *        const a = 'hello'
 *        a = 'goodbye'
 *
 * will throw error
 *
 * let -> allows for variables to be reassigned
 *  example ->
 *      let b = 'hello'
 *      b = 'goodbye
 *
 * will be good
 *
 *
 * it is good to use these to make your code more readable
 */

// 1. make variable
// var ____ = ______

// 2. log variable
// console.log(_____)
